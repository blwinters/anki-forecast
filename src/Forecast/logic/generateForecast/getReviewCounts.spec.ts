import { getReviewCounts } from './getReviewCounts'

describe('getReviewCounts', () => {
  describe('prioritization', () => {
    it('fetches interday learning cards', () => {
      //
    })

    it('then it fetches reviews', () => {
      //
    })
    it('then it fetches new', () => {
      //
    })
  })
})
